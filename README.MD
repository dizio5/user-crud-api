This project consists of an API implementation using REST principles. It also involves CRUD methodsto modify users data through PostgreSQL database.
To run properly this project, you need to install maven and postgresql. Run a local hosted databaseand add credentials to venv (otherwise, you can hard-code them on properties.yaml, not recommended)

Future features:
- JWT implementation
- More endpoints
- Frontend using React
